<%= form_with(model: poject) do |form| %>
  <% if poject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poject.errors.count, "error") %> prohibited this poject from being saved:</h2>

      <ul>
        <% poject.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
